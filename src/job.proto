syntax = "proto3";
package types;
import "template.proto";

message JobRequest {
    string name = 1;
    string description = 2;
    Trigger trigger = 3;
    ScanTemplate scanTemplate = 4;
}

message JobResponse {
}

service JobService {
  rpc Apply (JobRequest) returns (JobResponse) {}
}